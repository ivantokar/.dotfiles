onboarding = false

# Herdr retains the familiar terminal-workflow settings in a native config.

[theme]
name = "gruvbox"

auto_switch = false
[theme.custom]
panel_bg = "black"
accent = "cyan"
text = "white"

[terminal]
default_shell = "/bin/zsh"
shell_mode = "login"
# New panes and tabs inherit the focused pane's directory.
new_cwd = "follow"

[keys]
# Use Ctrl-S as the pane-management prefix.
prefix = "ctrl+s"
# Use familiar split bindings.
split_vertical = "prefix+shift+backslash"
split_horizontal = "prefix+minus"
# Herdr reloads its TOML configuration with the closest safe equivalent.
reload_config = "prefix+r"

# Hand directional navigation to the Herdr-aware Neovim action at pane edges.
[[keys.command]]
key = "ctrl+h"
type = "plugin_action"
command = "herdr-nvim-nav.left"

[[keys.command]]
key = "ctrl+j"
type = "plugin_action"
command = "herdr-nvim-nav.down"

[[keys.command]]
key = "ctrl+k"
type = "plugin_action"
command = "herdr-nvim-nav.up"

[[keys.command]]
key = "ctrl+l"
type = "plugin_action"
command = "herdr-nvim-nav.right"

[ui]
# Keep tabs at the top.
tab_bar_position = "top"
pane_borders = true
pane_gaps = false
sidebar_start_collapsed = true
agent_panel_sort = "spaces"
show_agent_labels_on_pane_borders = false

[ui.toast]
delivery = "herdr"
