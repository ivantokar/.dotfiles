# Preserve interactive history behavior; history, encryption keys, and sessions remain local.
enter_accept = true
search_mode = "daemon-fuzzy"

[sync]
records = true

[daemon]
enabled = true
autostart = true

[ai]
enabled = true
