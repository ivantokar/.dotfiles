---
description: Build the Xcode project using xcodebuild
---

Build the Xcode project. Run `xcodebuild` with appropriate parameters to compile the project. Show the build output and report any errors or warnings found.

If the build succeeds, run the post-build hook: `.claude/hooks/post-build.sh`

If the build fails, analyze the errors and suggest fixes.
