---
description: Clean Xcode build artifacts and derived data
---

Clean the Xcode project build artifacts by:
1. Running `xcodebuild clean` for the project
2. Optionally removing DerivedData directory
3. Clearing any cached build files

This helps resolve build issues caused by stale artifacts.
